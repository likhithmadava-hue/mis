# Used by `npm run build:netlify` (vite build --mode fresh).
#
# The desktop build ships two weeks of sample data so every chart has something
# to draw on the first run. A hosted copy is opened by people who are not you,
# and demo rows they did not enter are confusing — so this build seeds nothing.
VITE_FRESH_START=true
