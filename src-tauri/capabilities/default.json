{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the MIS window is allowed to ask the Rust side to do. The filesystem grant is for exports only: the user picks the destination in a native Save dialog, and MIS writes the file they asked for. The vault is denied outright — it is reached through the commands in `vault/`, which encrypt, hash-chain and audit every touch, and a frontend able to write those files directly would be a way around all three.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:webview:allow-internal-toggle-devtools",

    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-confirm",

    "fs:allow-read-file",
    "fs:allow-write-file",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$DOWNLOAD/**" },
        { "path": "$DOCUMENT/**" },
        { "path": "$DESKTOP/**" },
        { "path": "$HOME/**" }
      ],
      "deny": [{ "path": "$APPLOCALDATA/**" }]
    }
  ]
}
