## 2025-05-18 - Screen Reader Accessibility for Icon-Only Buttons
**Learning:** Icon-only buttons with `title` attributes are helpful for mouse users on hover, but require explicit `aria-label` attributes to ensure screen readers read meaningful descriptive labels.
**Action:** Always pair `title` on icon-only buttons with an explicit `aria-label` or dynamic context label.
